class CubeSortModel {
  String texto;

  CubeSortModel(this.texto) {
    texto =
        '   Cubesort é um algoritmo de ordenação paralela que constrói uma matriz multidimensional auto-equilibrada a partir das chaves a serem ordenadas. Como os eixos são de comprimento semelhante, a estrutura se assemelha a um cubo. Depois que cada chave é inserida, o cubo pode ser rapidamente convertido em uma matriz.';
  }
}
