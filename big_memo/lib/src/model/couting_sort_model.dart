class CoutingSortModel {
  String texto;
  String texto2;

  CoutingSortModel(this.texto, this.texto2) {
    texto =
        '   Na ciência da computação, a ordenação por contagem é um algoritmo para ordenar uma coleção de objetos de acordo com chaves que são pequenos inteiros positivos ; ou seja, é um algoritmo de ordenação de inteiros. Ele opera contando o número de objetos que possuem valores de chave distintos e aplicando a soma do prefixo nessas contagens para determinar as posições de cada valor de chave na sequência de saída. Seu tempo de execução é linear no número de itens e na diferença entre o valor máximo da chave e o valor mínimo da chave, portanto, é adequado apenas para uso direto em situações em que a variação nas chaves não é significativamente maior que o número de itens. É frequentemente usado como uma sub-rotina emradix sort , outro algoritmo de classificação, que pode manipular chaves maiores com mais eficiência.';
    texto2 = '';
  }
}
